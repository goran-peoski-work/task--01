{
  "name": "@task/shared",
  "version": "0.0.1",
  "type": "module",
  "exports": {
    "./*": {
      "types": "./dist/*.d.ts",
      "default": "./dist/*.js"
    }
  },
  "scripts": {
    "check:types": "tsc --noEmit",
    "fix:format": "prettier --write .",
    "fix": "npm run fix:format",
    "dev": "tsc --watch",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
